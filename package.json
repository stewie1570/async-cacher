{
  "name": "async-cacher",
  "version": "1.4.0",
  "description": "Cache async data sources.",
  "repository": {
    "type": "git",
    "url": "https://github.com/stewie1570/async-cacher"
  },
  "main": "build/index.js",
  "scripts": {
    "test": "jest --coverage --modulePathIgnorePatterns=build/",
    "tdd": "jest --watch",
    "build": "yarn run test && del-cli ./build && tsc",
    "prepare": "yarn run build",
    "prepublishOnly": "yarn run build"
  },
  "keywords": [
    "async",
    "cache"
  ],
  "author": "Stewart Anderson",
  "license": "GPL-3.0",
  "devDependencies": {
    "@babel/core": "^7.6.2",
    "@babel/preset-env": "^7.6.2",
    "@babel/preset-typescript": "^7.6.0",
    "@types/jest": "^24.0.18",
    "babel-jest": "^24.9.0",
    "del-cli": "^3.0.0",
    "jest": "^24.9.0",
    "typescript": "^3.6.3"
  },
  "dependencies": {},
  "files": [
    "build"
  ]
}
